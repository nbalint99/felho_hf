apiVersion: apps/v1
kind: Deployment
metadata:
  name: other-mongo-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: other-mongo
  template:
    metadata:
      labels:
        app: other-mongo
    spec:
      containers:
      - name: other-mongo
        image: mongo:4.4
        ports:
        - containerPort: 27017
        volumeMounts:
        - name: mongo-storage
          mountPath: /data/db
      volumes:
      - name: mongo-storage
        persistentVolumeClaim:
          claimName: mongo-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: other-mongo
spec:
  selector:
    app: other-mongo
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
